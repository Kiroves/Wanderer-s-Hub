import React from 'react'

const Results = () => {
    const res = sessionStorage.getItem('results') /
        console.log(res);
    return (
        <div>
            hj
        </div>
    )
}

export default Results
